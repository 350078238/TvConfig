server:
  port: 80

feign:
  okhttp:
    enabled: true
#feign:
#  hystrix:
#    enabled: true

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:7001/eureka/